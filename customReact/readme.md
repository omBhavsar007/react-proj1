This is basic custom react project, that helps us to understand how react's babel actually converting JSX into JS, because browser only understands JS. 

This all lines of code gives us simple example how bts in the react might be working, but a face that all the jsx is indeed converted into JS for browser to undertand.